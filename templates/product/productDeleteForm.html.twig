{% block productDeleteForm %}
    {{ form_start(productDeleteForm, {'action': path('productDelete'), 'attr': {'data-url': path('index')}}) }}
    {{ form_row(productDeleteForm.product) }}
    {{ form_end(productDeleteForm) }}
{% endblock %}
