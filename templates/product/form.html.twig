{% block form %}
        {% set form = mainPageManager.getProductAddForm().createView() %}
    {{ form_start(form, {'action': path('productAdd')}) }}
    {{ form_row(form.product) }}
    {{ form_end(form) }}
{% endblock %}
