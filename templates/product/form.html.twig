{% block form %}
        {% set form = pageManager.getProductAddForm().createView() %}
    {{ form_start(form, {'action': path('productAdd')}) }}
    {{ form_row(form.product) }}
    {{ form_end(form) }}
{% endblock %}
