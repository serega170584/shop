{% block form %}
    {{ form_start(form, {'action': path('productAdd11111')}) }}
    {{ form_row(form.productId) }}
    {{ form_end(form) }}
{% endblock %}
asdasdasdasd
<script>
    $(function(){
        $('.button-one').click(function(e){
            e.preventDefault();
            console.log('123');
        });
    });
</script>