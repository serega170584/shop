{% block form %}
    {{ form_start(form, {'action': path('productAdd')}) }}
    {{ form_row(form.productId) }}
    {{ form_end(form) }}
{% endblock %}
asdasdasdasd
<script>
    $(function(){
        $('.button-one').click(function(e){
            e.preventDefault();
            console.log('123');
        });
    });
</script>