{% extends 'base.html.twig' %}
{% block body %}
    <h1>Upload a category</h1>
    <p><b>{{ message }}</b></p>
    {{ form(form) }}
    <script src="{{ asset('js/upload-change.js') }}"></script>

    {% if path %}
        <script>
            document.addEventListener("DOMContentLoaded", () => {
                let filename = '{{ path }}';
                window.parent.document.getElementById('Category_image').setAttribute('value', filename);
                window.parent.document.getElementById('file_label').innerHTML = filename;
                window.parent.document.getElementById('image_trash').style.display = 'block'
                let inputGroup = window.parent.document.getElementById('Category_image').closest('.input-group');
                let image = window.parent.document.getElementById('category_image');
                if (image == undefined) {
                    let img = window.parent.document.createElement('img');
                    img.setAttribute('src', `/uploads/category/${filename}`);
                    img.setAttribute('id', 'category_image');
                    inputGroup.after(img);
                } else {
                    image.setAttribute('src', `/uploads/category/${filename}`);
                }
            });
        </script>
    {% endif %}
{% endblock %}

