{% extends 'frontend/page.html.twig' %}
{% block breadcrumbs %}
    <ul class="page-depth">
        <li><a href="{{ path('index') }}">Главная</a></li>
        <li><a href="{{ path('author-list', { slug: author.getSlug() }) }}">{{ author.getTitle() }}</a></li>
    </ul>
{% endblock %}
{% block page_content %}
    {% for product in products %}
        {{ block("listItem", "product/list-item.html.twig") }}
    {% endfor %}
{% endblock %}